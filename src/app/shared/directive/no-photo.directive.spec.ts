import { NoPhotoDirective } from './no-photo.directive';

describe('NoPhotoDirective', () => {
  it('should create an instance', () => {
    const directive = new NoPhotoDirective();
    expect(directive).toBeTruthy();
  });
});
